/**
 * Removed server-side PDF generation due to environment constraints.
 * Reports are now rendered as printable HTML at /reports/preview.
 * This file intentionally left empty to avoid route conflicts.
 */
export const dynamic = "error";